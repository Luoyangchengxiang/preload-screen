{
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "clean": "pnpm -r exec rimraf dist node_modules/.vite .turbo .rollup.cache",
    "clean:hard": "rimraf node_modules pnpm-lock.yaml && pnpm -r exec rimraf node_modules",
    "release": "turbo run build test && pnpm --filter preload-screen release"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.8"
  }
}